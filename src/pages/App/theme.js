import { createTheme } from '@material-ui/core';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#8bc34a',
    },
    secondary: {
      main: '#ffea00',
    },
    info: {
      main: '#fff',
    },
    success: {
      main: '#03a9f4',
    },
    background: {
      default: '#121212',
    },
  },
});
